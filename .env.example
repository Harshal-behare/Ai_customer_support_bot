# Copy this file to .env and fill in your OpenAI credentials.
# The application automatically loads environment variables from .env on startup.

# Required for LLM-powered responses. Without this key, the bot falls back to template replies.
OPENAI_API_KEY=sk-REPLACE_ME

# Optional: override the default model.
OPENAI_MODEL=gpt-4o-mini
